#include <stdio.h>
#include <math.h>
#include <conio.h>
int main ()
{ 	
	float a;
	printf ("nhap a :");
	scanf ("%f",&a);
	 int b;
	 b=int(a);
		if (a-b==0.5)
	printf (" day la so ban nguyen");
		else 
	printf (" day k phai la so ban nguyen ");
	return 0;
}
