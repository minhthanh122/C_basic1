#include <stdio.h>
#include <math.h>
int main ()
{ int a;
printf (" Nhap so a : ");
scanf ("%d",&a);
if ( a/sqrt (a) == sqrt (a) )
printf ("day la so chinh phuong");
else 
printf (" day khong phai la so chinh phuong");
return 0;

}	
